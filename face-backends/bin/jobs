#!/usr/bin/env ruby

require_relative "../config/environment"
require "solid_queue/cli"

arguments = ARGV.dup
if RUBY_PLATFORM.include?("darwin") && arguments.none? { |argument| argument.start_with?("--mode") }
  arguments << "--mode=async"
end

SolidQueue::Cli.start(arguments)
